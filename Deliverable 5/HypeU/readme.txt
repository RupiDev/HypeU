To run the server, install Node and run `npm install` then `node index.js`

To run the unit tests, run `node_modules/mocha/bin/mocha`

To run the system test, run the server normally and go to http://localhost:8080
